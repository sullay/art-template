{
  "name": "my-jsx",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "esbuild src/main.jsx --bundle --jsx-factory=art.h --jsx-fragment=art.Fragment --outfile=dist/test/out.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "vite": "^2.7.2"
  }
}